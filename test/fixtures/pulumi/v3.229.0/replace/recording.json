{
  "scenario": "replace",
  "description": "A new prefix on the pet and new content in the file, the file with deleteBeforeReplace. Two replaces with replace reasons, one in each order.",
  "cliVersion": "v3.229.0",
  "commands": [
    {
      "id": "preview",
      "argv": [
        "pulumi",
        "preview",
        "--json",
        "--non-interactive",
        "--color",
        "never",
        "--stack",
        "dev"
      ],
      "cwd": "network",
      "exitCode": 0,
      "stdout": "preview.stdout",
      "stderr": "preview.stderr",
      "stdoutFormat": "json"
    }
  ]
}
