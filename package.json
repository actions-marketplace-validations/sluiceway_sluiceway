{
  "name": "sluiceway",
  "version": "0.0.0",
  "description": "One GitHub issue that shows which infrastructure stacks have changes waiting, and deploys a stack when someone ticks its box.",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sluiceway/sluiceway.git"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build src/main.ts --target=node --format=esm --outfile=dist/index.js",
    "check:dist": "bun run build && git diff --exit-code -- dist && test -z \"$(git status --porcelain -- dist)\"",
    "check": "bun run lint && bun run typecheck && bun run test && bun run check:dist"
  },
  "dependencies": {
    "@actions/core": "3.0.1",
    "yaml": "2.9.1",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@types/bun": "1.3.14",
    "typescript": "7.0.2"
  }
}
